"use client";
import { FormEvent, useCallback, useEffect, useState } from "react";
type Item={name:string;option:string;unitPrice:number;quantity:number};type Message={id:number;sender:"customer"|"owner";body:string;createdAt:string};type Order={id:string;email:string;items:Item[];subtotal:number;status:string;createdAt:string;messages:Message[]};
export default function OwnerOrders(){const [orders,setOrders]=useState<Order[]>([]);const [open,setOpen]=useState<string>();const [drafts,setDrafts]=useState<Record<string,string>>({});const [error,setError]=useState("");const load=useCallback(async()=>{const response=await fetch("/api/owner/orders",{cache:"no-store"});if(response.ok)setOrders((await response.json()).orders)},[]);useEffect(()=>{load();const timer=setInterval(load,5000);return()=>clearInterval(timer)},[load]);const status=async(id:string,value:string)=>{const response=await fetch("/api/owner/orders",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id,status:value})});const data=await response.json();if(response.ok)setOrders(data.orders);else setError(data.error||"Status update failed.")};const send=async(event:FormEvent,id:string)=>{event.preventDefault();const body=drafts[id]?.trim();if(!body)return;const response=await fetch(`/api/owner/orders/${encodeURIComponent(id)}/messages`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({body})});const data=await response.json();if(response.ok){setOrders(data.orders);setDrafts(value=>({...value,[id]:""}))}else setError(data.error||"Message failed.")};return <section className="owner-orders"><div className="owner-orders-heading"><div><p className="eyebrow">ORDERS / PRIVATE CHAT</p><h2>Customer orders.</h2></div><button onClick={load}>Refresh</button></div>{error&&<div className="status">{error}</div>}{orders.length===0?<p className="orders-empty">No orders yet.</p>:<div className="orders-list">{orders.map(order=><article key={order.id}><button className="order-summary" onClick={()=>setOpen(open===order.id?undefined:order.id)}><span><small>{new Date(order.createdAt).toLocaleString()}</small><b>{order.id}</b><em>{order.email}</em></span><strong>${order.subtotal.toFixed(2)}</strong><i className={order.status}>{order.status}</i></button>{open===order.id&&<div className="owner-order-detail"><div className="owner-order-items">{order.items.map((item,index)=><p key={index}><span>{item.name} · {item.option} ×{item.quantity}</span><b>${(item.unitPrice*item.quantity).toFixed(2)}</b></p>)}</div><label>Order status<select value={order.status} onChange={event=>status(order.id,event.target.value)}><option value="pending">Pending payment</option><option value="paid">Paid</option><option value="completed">Completed</option><option value="cancelled">Cancelled</option><option value="closed">Close and hide from customer</option></select></label><div className="owner-messages">{order.messages.length===0?<p>No messages yet.</p>:order.messages.map(message=><div className={message.sender} key={message.id}><b>{message.sender==="owner"?"YOU":"CUSTOMER"}</b><p>{message.body}</p><small>{new Date(message.createdAt).toLocaleString()}</small></div>)}</div>{order.status!=="closed"&&<form onSubmit={event=>send(event,order.id)}><textarea placeholder="Reply privately…" maxLength={2000} value={drafts[order.id]||""} onChange={event=>setDrafts(value=>({...value,[order.id]:event.target.value}))}/><button>Send reply</button></form>}</div>}</article>)}</div>}</section>}
