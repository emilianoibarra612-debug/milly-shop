import { NextRequest, NextResponse } from "next/server";
import { addVouch, addWarrantyClaim, customerOrder, customerOrderWithEvents, messagesFor, orderCookie } from "@/lib/orders";
export const dynamic="force-dynamic";
export async function GET(request:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;const order=await customerOrderWithEvents(id,request.cookies.get(orderCookie(id))?.value);if(!order)return NextResponse.json({error:"Order not found or access denied."},{status:404});return NextResponse.json({order,messages:await messagesFor(id)})}
export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;if(!await customerOrder(id,request.cookies.get(orderCookie(id))?.value))return NextResponse.json({error:"Access denied."},{status:403});try{const {action,body}=await request.json();if(action==="vouch")await addVouch(id);else if(action==="warranty")await addWarrantyClaim(id,String(body||""));else throw new Error("Invalid order action.");return NextResponse.json({ok:true})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Could not update order."},{status:400})}}
